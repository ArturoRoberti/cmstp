# Test task with an unallowed arg (i.e. 'custom' instead of 'default')
some-task:       # Placeholder valid task to avoid eventual empty config
  enabled: true
some-other-task: # Invalid task
  enabled: true
  args:
    custom: [unallowed_arg]
